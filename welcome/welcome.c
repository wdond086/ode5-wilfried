#include <stdio.h>
#include "exercise.c"

int make_it_higher(int n) {
    return n + 100 + 20 + 7;
}

int main() {
    int name = 0;

    getUserChar();

    // while (1) {
    //     name = make_it_higher(name);
    //     // It means print out this number but its hexadecimal representation
    //     printf("Welcome to %04X\n", name);
    // }
}
